def PowMod(x = 2, y = 20, mod = 998244353):
    if y == 0:
        return 1
    if y == 1:
        return x
    tmp = PowMod(x, y // 2, mod)
    tmp *= tmp
    if y % 2 == 1:
        tmp *= x
    return tmp
print(PowMod())